#PRIME NUMBER QUESTION

import random
def randomprime(x,y):  
    
    x=1000
    
    while x==1000:
        
    control = 0
        
    c = int(random.randint(x,y))
        
        if c!=1:
            for i in range(2,j):
                if ( j%i==0 ):  
                    control=control+1  

            if (control!=0):
            p=1000
        
        else:
        return j
        break
        else:
            p=1000

for w in range(3): 
  for v in range(3):
        print(randomprime(1,1000), end=" ")    
