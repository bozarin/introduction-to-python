#PRIME NUMBER QUESTION

#PRIME NUMBER QUESTION

import random
def randomprime(x,y):  
    
    l=1000
    
    while l==1000:
        
    limit=0
        
    j = int(random.randint(x,y))
        
        if j!=1:
            for i in range(2,j):
                if ( j%i==0 ):  
                    limit=limit+1  

            if (limit!=0):
            p=1000
        
        else:
        return c
        break
        else:
            p=1000

for w in range(3): 
  for v in range(3):
        print(randomprime(1,1000), end=" ")  
